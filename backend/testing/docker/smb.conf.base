[global]
   workgroup = WORKGROUP
   server string = nonraid-webui test
   security = user
   map to guest = Bad User
   log file = /var/log/samba/log.%m
   max log size = 1000
   dns proxy = no

# === nonraid-webui:managed-shares:begin ===
# === nonraid-webui:managed-shares:end ===
